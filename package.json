{
  "name": "ytools",
  "version": "1.0.0",
  "description": "YTools — حاسبة، حاسبة قروض، محول عملات",
  "private": true,
  "scripts": {
    "start": "npx --yes serve www -l 5173",
    "android": "npx cap run android",
    "android:open": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
